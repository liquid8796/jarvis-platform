<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <Style x:Key="SessionFocus"><Setter Property="Control.Template"><Setter.Value><ControlTemplate><Rectangle Margin="-3" Stroke="{DynamicResource Text}" StrokeThickness="2" StrokeDashArray="2,1" SnapsToDevicePixels="True"/></ControlTemplate></Setter.Value></Setter></Style>
  <Style x:Key="SessionRow" TargetType="ListBoxItem" BasedOn="{StaticResource {x:Type ListBoxItem}}"><Setter Property="FocusVisualStyle" Value="{StaticResource SessionFocus}"/></Style>
  <Style x:Key="SessionBody" TargetType="TextBlock"><Setter Property="Foreground" Value="{DynamicResource Text}"/><Setter Property="FontSize" Value="13"/><Setter Property="TextWrapping" Value="Wrap"/></Style>
  <Style x:Key="SessionHint" TargetType="TextBlock" BasedOn="{StaticResource SessionBody}"><Setter Property="Foreground" Value="{DynamicResource Muted}"/><Setter Property="FontSize" Value="12"/></Style>
  <Style x:Key="SessionSelectableText" TargetType="TextBox" BasedOn="{StaticResource {x:Type TextBox}}">
    <Setter Property="IsReadOnly" Value="True"/><Setter Property="IsReadOnlyCaretVisible" Value="True"/><Setter Property="KeyboardNavigation.IsTabStop" Value="False"/>
    <Setter Property="Background" Value="Transparent"/><Setter Property="BorderThickness" Value="0"/><Setter Property="Padding" Value="0"/>
    <Setter Property="Foreground" Value="{DynamicResource Text}"/><Setter Property="CaretBrush" Value="{DynamicResource Text}"/><Setter Property="SelectionBrush" Value="{DynamicResource Accent}"/>
    <Setter Property="VerticalContentAlignment" Value="Center"/><Setter Property="Cursor" Value="IBeam"/><Setter Property="FocusVisualStyle" Value="{StaticResource SessionFocus}"/>
  </Style>
  <Style x:Key="SessionError" TargetType="TextBlock" BasedOn="{StaticResource SessionBody}">
    <Setter Property="Foreground" Value="{DynamicResource Danger}"/><Setter Property="Margin" Value="0,8,0,0"/>
    <Setter Property="AutomationProperties.LiveSetting" Value="Polite"/>
    <Style.Triggers><Trigger Property="Text" Value=""><Setter Property="Visibility" Value="Collapsed"/></Trigger></Style.Triggers>
  </Style>
  <Style x:Key="SessionCard" TargetType="Border" BasedOn="{StaticResource Card}">
    <Setter Property="Background" Value="{DynamicResource Panel}"/><Setter Property="BorderBrush" Value="{DynamicResource Line}"/><Setter Property="Padding" Value="20"/>
  </Style>
  <Style x:Key="SessionInput" TargetType="TextBox" BasedOn="{StaticResource {x:Type TextBox}}">
    <Setter Property="Background" Value="{DynamicResource Bg}"/><Setter Property="Foreground" Value="{DynamicResource Text}"/>
    <Setter Property="CaretBrush" Value="{DynamicResource Text}"/><Setter Property="BorderBrush" Value="{DynamicResource Line}"/>
    <Setter Property="FocusVisualStyle" Value="{StaticResource SessionFocus}"/>
    <Setter Property="Validation.ErrorTemplate" Value="{x:Null}"/>
    <Style.Triggers><Trigger Property="Validation.HasError" Value="True"><Setter Property="BorderBrush" Value="{DynamicResource Danger}"/></Trigger></Style.Triggers>
  </Style>
  <Style x:Key="SessionButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
    <Setter Property="FocusVisualStyle" Value="{StaticResource SessionFocus}"/>
    <Setter Property="Background" Value="{DynamicResource Panel}"/><Setter Property="Foreground" Value="{DynamicResource Text}"/><Setter Property="BorderBrush" Value="{DynamicResource Line}"/>
  </Style>
  <Style x:Key="SessionPrimary" TargetType="Button" BasedOn="{StaticResource SessionButton}"><Setter Property="Background" Value="{DynamicResource Accent}"/><Setter Property="Foreground" Value="{DynamicResource OnAccent}"/></Style>
  <DataTemplate x:Key="NumericSettingTemplate">
    <Grid Margin="0,16,0,0">
      <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="108"/></Grid.ColumnDefinitions>
      <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
      <StackPanel Margin="0,0,20,0"><TextBlock Text="{Binding Label}" Style="{StaticResource SessionBody}" FontWeight="SemiBold"/><TextBlock Text="{Binding Hint}" Style="{StaticResource SessionHint}" Margin="0,6,0,0"/></StackPanel>
      <TextBox Grid.Column="1" Text="{Binding Text, UpdateSourceTrigger=PropertyChanged, ValidatesOnNotifyDataErrors=True}" Style="{StaticResource SessionInput}" VerticalAlignment="Top" TextAlignment="Right" AutomationProperties.Name="{Binding Label}" AutomationProperties.HelpText="{Binding Hint}"/>
      <TextBlock Grid.Row="1" Grid.ColumnSpan="2" Text="{Binding Error}" Style="{StaticResource SessionError}"/>
    </Grid>
  </DataTemplate>
</ResourceDictionary>
